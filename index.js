const inquirer = require('inquirer');
const fs = require ('fs');
const { Circle, Triangle, Square } = 
inquirer
.prompt([
    {type: 'input',
     name: 'fillcolor',
     message: 'What color would you like to use to fill your circle?',
    }
])

.then
let
switch (key) {
    case value:
        
        break;

    default:
        break;
}